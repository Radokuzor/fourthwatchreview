[build]
builder = "nixpacks"
buildCommand = "npx --yes pnpm@10.4.1 install --frozen-lockfile && npx --yes pnpm@10.4.1 build"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
