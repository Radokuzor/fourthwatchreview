[phases.setup]
nixPkgs = ["nodejs_20"]

# npx runs pnpm without Corepack (avoids signature/key mismatches on Nix Node images).
[phases.install]
cmds = ["npx --yes pnpm@10.4.1 install --frozen-lockfile"]

[phases.build]
cmds = ["npx --yes pnpm@10.4.1 build"]

[start]
cmd = "node dist/index.js"
